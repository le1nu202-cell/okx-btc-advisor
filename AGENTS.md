# AGENTS.md

## 项目定位

这是一个仅供本人使用的 BTC 手动交易辅助工具。

它不是自动量化交易系统，不负责预测 BTC 涨跌，不承诺盈利，也不自动下单。

项目的首要目标是：

1. 帮助用户快速制定交易计划。
2. 精确计算分批加仓后的风险。
3. 提醒用户执行预先制定的止盈和止损。
4. 记录用户真实交易，并统计该交易方式是否有效。
5. 尽量减少用户临场随意扩大仓位、重复补仓或移动止损。
6. 让用户在几秒内看清一笔交易最坏会亏多少 USDT。

## 用户的实际交易方式

默认交易品种：

* OKX BTC-USDT-SWAP
* USDT 本位永续合约
* 账户权益默认 80 USDT，但允许修改
* 杠杆默认 66 倍，但允许修改
* 手动交易，不自动执行

典型流程：

1. 用户主观选择初始开仓位置。
2. 初始保证金通常为账户权益的 3% 至 5%。
3. 用户预先设置第一压力位。
4. 到达第一压力位后，用户可能增加初始仓位两倍的仓位。
5. 每笔交易最多加仓一次。
6. 加仓后若价格回到手续费调整后的均价区域，用户减少与加仓数量相同的仓位。
7. 如果价格继续突破第二压力位，全部止损。
8. 如果价格先到止盈位，直接全部止盈。
9. 软件只负责计算、展示、提醒和记录，最终操作由用户确认。

## 永久安全限制

除非用户以后明确修改本文件，否则必须遵守：

* 不接入 OKX API Key。
* 不读取真实账户、余额或持仓。
* 不添加自动下单、改单、撤单功能。
* 不保存交易所密钥。
* 只使用 OKX 公共行情接口。
* 服务只监听本机地址。
* 不把项目部署到公网。
* 不声称任何策略能够稳定盈利。
* 不因为回测结果不好而删除失败记录。
* 不通过反复调参把失败策略包装成成功策略。
* 不擅自增加高风险交易功能。
* 不擅自把提醒功能改成自动执行功能。

## 产品优先级

优先级从高到低：

1. 当前交易计划
2. 加仓后的均价和总风险
3. 第二压力位止损亏损
4. 手续费和滑点后的真实保本价
5. 最大允许亏损反推仓位
6. 状态提醒
7. 交易日志和真实统计
8. 历史 Replay 训练
9. 技术指标辅助
10. 新闻和自动方向评分

自动方向建议、新闻评分和原有策略回测不能占据首页核心位置。

## 交互原则

页面应适合本人日常快速使用：

* 中文界面。
* 不堆砌专业术语。
* 重要风险优先显示绝对 USDT，再显示账户百分比。
* 默认值尽量符合用户的常用设置。
* 打开页面后，应在几秒内完成一笔交易计划。
* 不要求用户在多个页面来回切换。
* 所有高级研究内容放在可折叠区域或次级页面。
* 危险数字必须醒目，但不能隐藏用户输入或强行修改计划。
* 所有真实成交状态必须由用户确认，不能仅根据行情自动认定用户已经成交。
* 不制作只有视觉效果、没有实际功能的按钮、卡片或设置项。
* 如果某个功能尚未实现，必须明确标记为未实现，不能伪装成已生效。

## 交易计划必须包含

* 做多或做空
* 初始开仓价
* 第一压力位或加仓价
* 第二压力位或硬止损价
* 止盈价
* 账户权益
* 杠杆
* 初始保证金或资金比例
* 加仓倍数
* Maker 手续费
* Taker 手续费
* 预估滑点
* 最大允许亏损
* 当前计划状态

## 风险计算要求

必须按每次成交价分别计算 BTC 数量，然后根据实际数量计算加权均价。

不要直接对价格进行简单平均。

至少计算：

* 初始 BTC 数量
* 加仓 BTC 数量
* 总 BTC 数量
* 初始名义仓位
* 加仓名义仓位
* 总名义仓位
* 加仓后均价
* 毛保本价
* 手续费后保本价
* 止盈净利润
* 止损净亏损
* 最大亏损占账户比例
* 盈亏比
* 预计手续费
* 预计滑点
* 最大允许亏损对应的初始仓位
* 加仓后价格反向移动 0.1%、0.5% 和 1% 时的预计亏损

做多和做空必须分别实现并测试。

计算结果必须明确区分：

* 保证金
* 名义仓位
* BTC 数量
* 毛盈亏
* 手续费
* 滑点
* 净盈亏

不能把使用保证金比例误写成账户实际风险比例。

## 多代理与 Subagents 协作规则

对于复杂、可以拆分并行的任务，应主动使用多个 Subagents。

不要让主代理独自完成所有探索、实现、测试和审查工作。

### 应当使用 Subagents 的情况

出现以下任一情况时，应考虑并行委派：

* 需要同时检查前端、后端和数据库。
* 需要调研多个第三方开源组件。
* 需要同时检查数学公式、代码实现和测试覆盖。
* 需要同时进行安全审查、代码质量审查和产品验收。
* 需要阅读大量文件或较大的代码库。
* 需要排查多个相互独立的问题。
* 任务预计会修改多个相互独立的模块。
* 需要完成较大的版本升级或架构调整。

小型文案修改、单个明确 Bug 或只涉及一个文件的简单任务，不必为了形式强行创建 Subagents。

### 推荐的代理分工

根据任务性质选择合适的分工，不要求每次全部创建。

#### 1. Explorer Agent

负责只读探索：

* 阅读代码库结构
* 查找已有实现
* 定位相关文件
* 识别数据流
* 识别重复功能
* 检查当前依赖
* 返回文件路径和实现摘要

Explorer 默认不修改代码。

#### 2. Risk Math Agent

负责交易和风险公式：

* 验证做多与做空公式
* 验证加权均价
* 验证手续费后保本价
* 验证滑点
* 验证最大亏损反推仓位
* 提供固定测试向量
* 检查极端输入和边界条件

该代理不得仅凭直觉接受公式，必须给出可复现计算。

#### 3. Backend Agent

负责：

* FastAPI 接口
* SQLite 模型与迁移
* 状态机
* 数据持久化
* OKX 公共行情
* 后端单元测试
* 数据恢复和异常处理

#### 4. Frontend Agent

负责：

* React 和 TypeScript
* 交易计划表单
* 风险面板
* 图表价格线
* 状态展示
* 提醒交互
* 响应式界面
* 前端测试

#### 5. Test and Review Agent

负责只读或独立测试审查：

* 查找缺失测试
* 检查未来数据泄漏
* 检查状态不一致
* 检查异常输入
* 检查刷新和重启恢复
* 检查数据过期保护
* 检查前后端字段契约
* 检查回归风险

#### 6. Security and Dependency Agent

负责：

* 检查是否意外加入 API Key 或私有交易接口
* 检查服务监听地址
* 检查新增依赖许可证
* 检查第三方脚本和远程资源
* 检查潜在凭据泄漏
* 检查依赖维护状态
* 更新 THIRD_PARTY_NOTICES.md 所需信息

#### 7. Product Acceptance Agent

负责从用户的实际使用流程出发验收：

* 创建交易计划是否够快
* 风险数字是否容易看错
* 加仓后风险是否足够醒目
* 状态按钮是否真实生效
* 页面是否仍然像研究仪表盘
* 是否存在无用或重复信息
* 是否能够完成完整的开仓、加仓、减仓、止盈和止损流程

### 并行工作原则

* 优先并行执行只读探索、依赖调研、公式复核、测试分析和代码审查。
* 多个代理可以同时读取同一文件。
* 不允许多个代理同时修改同一个文件。
* 不允许多个代理同时修改高度耦合、容易产生冲突的模块。
* 如果需要并行写代码，必须提前为每个代理分配互不重叠的文件或目录。
* 数据库模型、共享类型、API 契约和核心计算模块原则上由一个代理统一修改。
* 其他代理应先返回建议，由主代理统一整合。
* 不允许 Subagent 再无限创建更多下级代理。
* 不要为了消耗额度而无意义地增加代理数量。
* 通常同时使用 2 至 4 个代理即可。
* 只有大型审查或明确可拆分任务才考虑更多代理。

### 主代理职责

主代理必须：

1. 先明确任务拆分方案。
2. 为每个 Subagent 指定清楚的边界和输出。
3. 说明哪些代理只读，哪些代理允许修改。
4. 等待所有必要代理完成。
5. 汇总结果，不能直接拼接各代理原始输出。
6. 处理不同代理之间的冲突意见。
7. 统一检查最终代码。
8. 运行完整测试。
9. 检查工作区中是否存在意外修改。
10. 在最终回复中说明使用了哪些 Subagents、分别负责什么、发现了什么。

### 推荐的并行模式

对于大型功能，优先采用：

第一轮并行：

* Explorer：检查现有实现
* Risk Math：复核公式
* Security/Dependency：检查组件和许可证
* Product Acceptance：检查当前产品问题

主代理汇总结论并确定架构。

第二轮实施：

* Backend Agent：修改后端范围
* Frontend Agent：修改前端范围

前后端开始写代码前，必须先固定接口字段和数据契约。

第三轮并行验收：

* Test and Review Agent：运行和审查测试
* Security Agent：安全回归检查
* Product Acceptance Agent：浏览器实际流程验收

主代理最后统一修复、运行完整验证并给出报告。

## 第三方组件原则

* 优先使用现有技术栈。
* 添加第三方依赖前，先检查许可证、维护状态和项目兼容性。
* 优先使用 MIT、BSD 或 Apache 等许可证清晰的开源组件。
* 不复制闭源产品源码。
* 必须在 THIRD_PARTY_NOTICES.md 记录新增组件、版本、用途、许可证和署名要求。
* 使用 TradingView Lightweight Charts 时，遵守其许可证、NOTICE 和署名要求。
* 不为了实现一个小功能引入大型交易机器人框架。
* 不在没有明确收益的情况下重写现有稳定模块。
* 不引入同时承担图表、账户、下单和自动策略的大型平台作为项目核心。
* 只复用成熟项目中真正需要的底层能力。

## 代码质量要求

* 保持前后端类型和字段一致。
* 核心风险公式只能有一个权威实现来源。
* 不在多个组件里复制同一套风险公式。
* 所有金额和数量必须明确单位。
* 金融计算避免直接依赖不受控制的浮点比较。
* 对价格、数量、比例、杠杆和手续费进行输入验证。
* 新增数据库字段时必须考虑旧数据兼容。
* 不删除已有失败回测结果。
* 不破坏一键启动和本地运行方式。
* 不提交临时日志、密钥、数据库文件、构建缓存或用户隐私数据。

## 开发流程

每个较大任务按以下顺序执行：

1. 阅读 AGENTS.md、README.md 和 PROGRESS.md。
2. 检查当前实现，不重复开发已有功能。
3. 判断是否适合使用 Subagents。
4. 如果适合，先拆分独立任务并并行委派。
5. 写出简短实施计划。
6. 明确哪些现有开源组件将被复用。
7. 固定前后端接口和风险公式。
8. 实现最小可用版本。
9. 添加测试。
10. 运行后端测试、前端测试、类型检查和生产构建。
11. 进行独立代码审查和产品流程验收。
12. 更新 README.md 和 PROGRESS.md。
13. 清楚说明完成内容、限制和下一步。

## 测试要求

风险计算至少覆盖：

* 做多
* 做空
* 初始价与加仓价不同
* 不同加仓倍数
* Maker 手续费
* Taker 手续费
* 滑点
* 最大亏损反推仓位
* 非法价格顺序
* 零价格
* 零数量
* 极端杠杆
* 很小账户权益
* 加仓后均价
* 手续费后保本价
* 部分减仓后的结果

状态机至少覆盖：

* 每笔计划最多加仓一次
* 未确认成交不得自动改变真实成交状态
* 刷新页面后恢复
* 重启服务后恢复
* 数据过期时停止新提醒
* 数据恢复后正确恢复提醒
* 止盈、止损和取消后的终止状态
* 不允许终止计划继续加仓

## 验收底线

任何新功能不得破坏：

* OKX 公共行情连接
* 数据缺口保护
* 过期行情保护
* SQLite 数据恢复
* 一键启动
* 原有测试
* 本机安全边界
* 已有失败回测记录
* 原有历史数据

最终交付前必须完成：

* Python compileall
* 后端测试
* 前端测试
* TypeScript 类型检查
* Vite 生产构建
* PowerShell 脚本检查
* 浏览器实际流程验收
* 安全边界复核
* 工作区意外文件检查

## 最终报告格式

完成较大任务后，最终回复必须包含：

1. 本次目标。
2. 使用了哪些 Subagents。
3. 每个 Subagent 的职责。
4. 采用了哪些第三方组件。
5. 修改了哪些主要文件。
6. 风险公式和接口如何实现。
7. 测试和构建结果。
8. 浏览器验收结果。
9. 安全边界是否保持。
10. 尚未实现的内容和已知限制。

不得只说“已完成”而不提供验证依据。
